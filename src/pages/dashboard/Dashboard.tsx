import React from 'react';
import { BaseLayoutPage } from '../../shared/layout';

export const Dashboard = () => {
  return (
    <BaseLayoutPage title="Home" navBar={<>Start Page</>}>
      test
    </BaseLayoutPage>
  );
};
